# 版本记录

每次发布在这里加一行。**只追加，不删除历史记录。**

| 版本(tag) | 日期 | 对应游戏原文 | 对应零协包 | 补丁文件数 | 说明 |
| --- | --- | --- | --- | ---: | --- |
| （未打 tag） | 2026-09-24 | Steam build 25345468 | 2026092102 | 0 | 仓库建立。此时零协汉化已覆盖全部文本，**无待补内容**，故不打 tag——避免有人从 Releases 下到空包 |

---

## 列的含义

| 列 | 含义 |
| --- | --- |
| **版本(tag)** | `patch/` 内容对应的 git tag。用 `git checkout <tag>` 可取回该版补丁 |
| **对应游戏原文** | 本版补丁是按哪一版游戏原文翻译的。游戏更新后原文若变化，旧补丁可能不再适用 |
| **对应零协包** | 翻译时参照的零协汉化版本。零协更新后，其已有译文优先于本补丁 |
| **补丁文件数** | `patch/` 里实际的文件数（不含 `README.md`） |
| **说明** | 这一版补了什么 |

## 为什么要记「对应游戏原文」

游戏更新会改动原文。原文一变，基于旧原文的译文就可能语义不符——
这时**不能继续用旧补丁**，必须按新原文重新翻译。

所以每一版补丁都绑定一个原文版本。安装前请对照这里确认版本是否匹配。
